[workspace]
authors = ["Tobias Fischer <info@tobiasfischer.info>"]
channels = ["https://prefix.dev/conda-forge"]
platforms = ["osx-arm64", "linux-64", "win-64"]

[tasks]
compare-completeness = { cmd = "python utils/compare_pkg_completeness.py", description = "Add two arguments to give it the DISTRO and CHANNEL"}
serve = { cmd = "zensical serve", description = "Serve the docs locally" }
build-docs = { cmd = "zensical build --strict", description = "Build the docs" }
gh-deploy = { cmd = "ghp-import -n -p -f -m 'Deploy docs' site", depends-on = ["build-docs"], description = "Build the docs and publish them to the gh-pages branch" }

[dependencies]
zensical = ">=0.0.53,<0.0.54"
# Provides the gh-pages push that `mkdocs gh-deploy` used to do
ghp-import = ">=2.1.0,<3"
# Required by copy-staging-packages.yml
requests = ">=2.34.2,<3"
anaconda-client = ">=1.14.1,<2"
